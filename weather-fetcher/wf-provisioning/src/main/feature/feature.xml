<?xml version="1.0" encoding="UTF-8"?>
<features name="weather-fetcher-v:${project.version}" xmlns="http://karaf.apache.org/xmlns/features/v1.6.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://karaf.apache.org/xmlns/features/v1.6.0">

    <repository>mvn:org.apache.cxf.karaf/apache-cxf/${apache-cxf.version}/xml/features</repository>
    <repository>mvn:org.apache.aries.jax.rs/org.apache.aries.jax.rs.features/${aries.jax.rs.whiteboard.version}/xml</repository>

    <feature name="wf-jpa" version="${project.version}">
        <bundle dependency="true">mvn:org.mariadb.jdbc/mariadb-java-client/${org.mariadb.jdbc.version}</bundle>
<!--        <feature>transaction</feature>-->
        <feature dependency="true">jndi</feature>
<!--        <feature>pax-jdbc-config</feature>-->
        <feature dependency="true">pax-jdbc-pool-dbcp2</feature>
        <feature dependency="true">jdbc</feature>
        <feature dependency="true">aries-blueprint</feature>
        <feature dependency="true">jpa</feature>
        <feature dependency="true">hibernate</feature>
        <feature dependency="true">pax-jdbc-config</feature>
        <bundle>mvn:com.wfproject/wf-jpa/${project.version}</bundle>
        <capability>
<!--            osgi.service;objectClass=javax.sql.DataSource;effective:=active;osgi.jndi.service.name=request_stats_datasource-->
            osgi.service;effective:=active;objectClass=javax.persistence.EntityManagerFactory;osgi.unit.name=request-stats-mariadb
        </capability>
    </feature>

    <feature name="wf-rest" version="${project.version}">
        <bundle>mvn:com.wfproject/wf-rest/${project.version}</bundle>
    </feature>
    <feature name="weather-fetcher" version="${project.version}">
        <requirement>osgi.service;effective:=active;filter:=(objectClass=org.osgi.service.http.HttpService)</requirement>
        <bundle>mvn:com.wfproject/wf-rest-impl/${project.version}</bundle>
        <feature dependency="true">aries-blueprint</feature>
        <feature version="${apache-cxf.version}" dependency="true">cxf-jaxrs</feature>
        <bundle dependency="true">mvn:com.fasterxml.jackson.core/jackson-core/${fasterxml-jackson.version}</bundle>
        <bundle dependency="true">mvn:com.fasterxml.jackson.core/jackson-annotations/${fasterxml-jackson.version}</bundle>
        <bundle dependency="true">mvn:com.fasterxml.jackson.core/jackson-databind/${fasterxml-jackson.version}</bundle>
        <bundle dependency="true">mvn:com.fasterxml.jackson.jaxrs/jackson-jaxrs-base/${fasterxml-jackson.version}</bundle>
        <bundle dependency="true">mvn:com.fasterxml.jackson.jaxrs/jackson-jaxrs-json-provider/${fasterxml-jackson.version}</bundle>
        <bundle dependency="true">mvn:org.mapstruct/mapstruct/${org.mapstruct.version}</bundle>

        <feature dependency="true">http</feature>
        <feature version="${project.version}">wf-rest</feature>
        <feature version="${project.version}">wf-jpa</feature>

        <configfile finalname="/etc/api.properties.cfg">mvn:com.wfproject/wf-provisioning/${project.version}/cfg/api-configuration</configfile>
        <configfile finalname="/etc/datasource.properties.cfg">mvn:com.wfproject/wf-provisioning/${project.version}/cfg/db-configuration</configfile>
    </feature>
</features>